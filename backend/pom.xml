<?xml version="1.0" encoding="UTF-8"?>
<!--
  Notas sobre Spring Boot 4 (pesquisado em jun/2026, depois do corte de
  conhecimento do Claude - este projeto nao foi compilado no sandbox por
  falta de acesso ao Maven Central, rode "mvn clean compile" como primeiro
  passo ao clonar):

  - "spring-boot-starter-web" foi renomeado para "spring-boot-starter-webmvc".
  - Flyway agora precisa do starter dedicado "spring-boot-starter-flyway"
    (antes bastava ter flyway-core no classpath).
  - "spring-boot-starter-data-jpa" e "spring-boot-starter-security"
    continuam com o mesmo nome de sempre.
  - Spring Security 7 (que vem com o Boot 4) habilita CSRF por padrao para
    metodos como POST/PUT/DELETE - por isso o SecurityConfig ja desabilita
    isso explicitamente, senao toda escrita na API voltaria 403.
  - Guia oficial de migracao, caso algo precise de ajuste:
    https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-4.0-Migration-Guide
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.wecti</groupId>
    <artifactId>wecti-api</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>wecti-api</name>
    <description>API de controle de acesso a palestras e eventos - WECTI</description>

    <properties>
        <java.version>17</java.version>
        <lombok.version>1.18.46</lombok.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
            <version>12.4.0</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.0.3</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>core</artifactId>
            <version>3.5.3</version>
        </dependency>
        <dependency>
            <groupId>com.google.zxing</groupId>
            <artifactId>javase</artifactId>
            <version>3.5.3</version>
        </dependency>
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.3</version>
        </dependency>
        <!--
            Renderizacao do certificado a partir do HTML/CSS aprovado, em vez
            de desenhar tudo na mao com a API do PDFBox: e o que permite o PDF
            sair identico ao modelo (mesmas fontes, mesmo SVG do logo, mesmas
            medidas), em vez de uma aproximacao.

            Usamos o artefato "io.github.openhtmltopdf" (fork mantido), nao o
            "com.openhtmltopdf" original: so este tem build para PDFBox 3, que
            e a versao ja usada no projeto - o original ainda depende de
            PDFBox 2 e conflitaria.

            svg-support (Batik) e o que desenha os SVGs inline do layout
            (faixa diagonal do topo, asteriscos e o logo da UNICID).
        -->
        <dependency>
            <groupId>io.github.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-pdfbox</artifactId>
            <version>1.1.82</version>
        </dependency>
        <dependency>
            <groupId>io.github.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-svg-support</artifactId>
            <version>1.1.82</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Banco em memoria, so para o teste que verifica se a aplicacao
             sobe (WectiApiApplicationTests). Sem ele esse teste exigiria um
             MySQL de verdade, e era por isso que ele falhava e o build
             precisava de -DskipTests. Nao vai para o .jar (scope test). -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!--
                        fork=true forca o javac a rodar como processo externo
                        (em vez de in-process na JVM do proprio Maven) - sem
                        isso, o Lombok nao consegue aplicar seus hacks de
                        reflexao sobre as classes internas do javac no JDK
                        usado neste projeto, e as anotacoes @Data/@Builder
                        silenciosamente nao geram nada.
                    -->
                    <fork>true</fork>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                    <!--
                        Ativa o perfil "dev" por padrao quando voce roda
                        "mvn spring-boot:run" localmente, sem precisar passar
                        -Dspring-boot.run.profiles=dev toda vez. So afeta este
                        goal (execucao local via Maven) - o JAR gerado por
                        "mvn clean package" (o que vai pra hospedagem) nao
                        ativa nenhum perfil sozinho, continua exigindo as
                        variaveis de ambiente reais em producao.
                    -->
                    <profiles>
                        <profile>dev</profile>
                    </profiles>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
